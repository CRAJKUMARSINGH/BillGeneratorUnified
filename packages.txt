# System packages for Streamlit Cloud
# Only packages that are available and work reliably on Streamlit Cloud

# Font support (essential for PDF generation)
fonts-liberation
fonts-dejavu-core

# Image processing (for reportlab)
libjpeg-dev
libpng-dev

# Note: Chrome, wkhtmltopdf, and WeasyPrint dependencies are not available on Streamlit Cloud
# The app will use xhtml2pdf and reportlab which work without system dependencies
